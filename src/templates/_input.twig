{% import "_includes/forms" as forms %}

{{ forms.textField({
    value: readTime.rounded,
    class: 'text fullwidth',
    disabled: true,
    readonly: true,
    instructions: "Reading time is automatically calculated on save, based on a reading speed of #{wpm} wpm"
}) }}
